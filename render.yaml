services:
  - type: web
    name: essay-club
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/essays
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000